# S0 orchestrator

done: contract, policy, runner (12 real-sandbox tests), gateway (8 tests), store + console (17 tests), corpus, edge config, docs; stdio smoke run and the two demo beats verified end to end (host run leaks the canary, firewalled run is BLOCKED).
blocked on: nothing.
next: rehearse the demo (docs/demo-script.md), then a real `wasmer deploy` to validate app.yaml.

## Gate log

- Gate 1 (runner merged): passed. Network interception is prototype-level on the SDK's NodeNetworkBridge; every DNS resolve and TCP connect is attributed to a run through a leased Wasmer client.
- Gate 2 (full path): passed. Claude-Code-style stdio launch of the gateway, real runner, sqlite store; BLOCKED result carries violations and a trace id.
- Gate 3 (freeze): passed. Corpus 9/9 on the real runner. The Node postinstall scenario exposed that the Node guest's fetch bypasses the socket bridge (and the SDK's disabled mode); fixed by guarding undici's dispatcher in every SDK worker plus exclusive Node runs. Gateway stdio smoke re-verified after the fix.

## Deviations from the plan

- No bash guest: sharrattj/bash crashes and coreutils fails to download in @wasmer/sdk 0.13, so run_shell only translates echo/cat/ls/pwd to Python.
- Node guest needs `--experimental-wasm-jspi`, and Node refuses it in NODE_OPTIONS, so the gateway is launched with `node` directly.
- Memory limits are recorded on the policy but not enforced (no SDK option).
- Browser replay was dropped; the console is read-mostly with a run form.
