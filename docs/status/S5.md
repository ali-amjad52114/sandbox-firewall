done: app.yaml (Edge app + 15-min eval cron + email cron), scripts/email-report.sh, README.md, docs/demo-script.md written against the Wasmer Edge docs (configuration, configuration/jobs, learn/email, learn/databases pages and the postgres-cronjobs-email blog post).
blocked on: the exact Edge start-command form for a plain Node app with the JSPI flag (the docs only cover framework autodetection and the WinterCG worker; cli_args vs NODE_OPTIONS is a best guess) and the working directory the cron jobs see for data/last-eval.md; both are flagged in app.yaml comments and cannot be verified until gateway/console/evals land and someone runs `wasmer deploy`.
next: run `wasmer deploy` once apps/console builds, check `wasmer app logs` for the JSPI flag being honoured, confirm the jobs fire on `*/15 * * * *` and that sendmail is on PATH for the email job; then set REPORT_TO/REPORT_FROM and the real owner in app.yaml.
